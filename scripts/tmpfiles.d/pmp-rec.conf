# tmpfiles.d config for pmp-rec runtime directories
# Installed to /usr/lib/tmpfiles.d/pmp-rec.conf
# See tmpfiles.d(5)

# Create /run/pmp-rec with correct ownership on boot
d /run/pmp-rec         0755 root root -
d /run/pmp-rec/sessions 0755 root root -
