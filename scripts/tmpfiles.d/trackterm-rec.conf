# tmpfiles.d config for trackterm-rec runtime directories
# Installed to /usr/lib/tmpfiles.d/trackterm-rec.conf
# See tmpfiles.d(5)

# Create /run/trackterm-rec with correct ownership on boot
d /run/trackterm-rec         0755 root root -
d /run/trackterm-rec/sessions 0755 root root -
