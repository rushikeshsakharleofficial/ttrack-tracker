[Unit]
Description=Percona Terminal Session Recorder Daemon
Documentation=https://github.com/percona/percona-monitoring-plugins
After=network.target
Requires=pmp-recd.socket

[Service]
Type=notify
ExecStart=/usr/libexec/pmp-recd -c /etc/pmp-rec/recd.conf
ExecReload=/bin/kill -HUP $MAINPID
Restart=always
RestartSec=5s
TimeoutStopSec=30s
WatchdogSec=60s

# Run as root to write to /var/lib/pmp-rec (owned root:pmp-audit)
User=root
Group=root

# Hardening
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/pmp-rec /run/pmp-rec
PrivateTmp=yes
NoNewPrivileges=yes
RestrictAddressFamilies=AF_UNIX
RestrictNamespaces=yes
RestrictRealtime=yes
CapabilityBoundingSet=CAP_CHOWN CAP_FOWNER CAP_DAC_OVERRIDE CAP_SETUID CAP_SETGID
AmbientCapabilities=
LockPersonality=yes
MemoryDenyWriteExecute=yes
SystemCallFilter=@system-service
SystemCallErrorNumber=EPERM

[Install]
WantedBy=multi-user.target
